# Django settings
DEBUG=1
SECRET_KEY=change_me_in_production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings - SQLite (default for local development)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# Database settings - PostgreSQL (used by Docker setup)
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=xfive
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_HOST=db
# DATABASE_PORT=5432

# For URL connection string format (alternative to individual settings)
# DATABASE_URL=postgres://postgres:postgres@db:5432/xfive 